/// asignar_adyacencias(posicion,arriba,abajo,derecha,izquierda)

with(objMemoriaBloque) {
    if (posicion == argument0) {
        arriba = argument1;
        abajo = argument2;
        derecha = argument3;
        izquierda = argument4;
    }
}
