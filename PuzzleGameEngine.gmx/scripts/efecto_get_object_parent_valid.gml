/// efecto_get_object_parent_valid(object_get_parent(object))

/*
    Script especificamente hecho debido a la irregularidad
    existente en la funciona variable_instance_exists (fucking yoyogames)
    que verifica si el parent del object al que le aplicamos 
    el efecto es un parent valido.
    
    Por probar: PROBAR variable_instance_exists en el nuevo GMS1 1.772
*/


